<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Ganti Password</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans min-h-screen flex items-center justify-center">
  <div class="bg-white p-8 rounded-lg shadow-md max-w-md w-full">
    <h2 class="text-xl font-bold mb-4 text-blue-700">🔒 Ganti Password</h2>

    <% if (error) { %>
      <div class="bg-red-100 text-red-700 px-4 py-2 rounded mb-4 text-sm">
        <%= error %>
      </div>
    <% } %>
    <% if (success) { %>
      <div class="bg-green-100 text-green-700 px-4 py-2 rounded mb-4 text-sm">
        <%= success %>
      </div>
    <% } %>

    <form method="POST" action="/change-password" class="space-y-4">
      <div>
        <label class="block text-sm font-medium">Password Lama</label>
        <input type="password" name="currentPassword" required class="w-full border p-2 rounded" />
      </div>
      <div>
        <label class="block text-sm font-medium">Password Baru</label>
        <input type="password" name="newPassword" required class="w-full border p-2 rounded" />
      </div>
      <button class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700 transition">
        Simpan Perubahan
      </button>
    </form>

    <div class="text-center mt-4">
        <a href="<%= role === 'admin' ? '/admin/dashboard' : '/operator/dashboard' %>">
        ← Kembali ke Dashboard
        </a>
    </div>
  </div>
</body>
</html>
